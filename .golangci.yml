run:
  
  # include test files or not, default is true
  tests: false

  skip-dirs:
    - cmd/intergrationtests

linters:
  enable:
    - misspell
    - gosec
    - goconst
    - interfacer
    - golint